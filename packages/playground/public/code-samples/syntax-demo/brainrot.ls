skibidi vibes = [72, 85, 91, 64, 88];

gyatt mid(arr) {
  skibidi tot = 0;
  grind (skibidi i = 0; i < len(arr); i++) {
    tot += arr[i];
  }

  yeet tot / len(arr);
}

skibidi vibe = mid(vibes);
yap("Vibe check: " + str(vibe));

sus (vibe >= 80) {
  yap("W rizz");
} cringe {
  yap("L rizz, skill issue");
}
